<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Desafio - BackEnd</title>
    <!-- BOOTSTRAP CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
</head>
<body>
    <!-- HEADER -->
    <%- include("../partials/header.ejs") %> 
    <!-- BODY -->
    <h1>E-Commerce - Risk Store</h1>
    <br>
    <%- include("../partials/link.ejs") %>
    <br> 
    <h3>Estos son nuestros productos cargados:</h3>
    <br>
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Title</th>
                <th scope="col">Price</th>
                <th scope="col">Image</th>
            </tr>
        </thead>
        <% if (listProducts) { %>
            <tbody>
                <% allProducts.forEach((prod) => { %> 
                    <tr>
                        <td scope="row"><%= prod.id %></td>
                        <td><%= prod.title %></td>
                        <td><%= prod.price %></td>
                        <td><img src=<%= prod.thumbnail %> alt=<%= prod.title %> style="width:8rem;"></td>
                    </tr>
                <% }); %>
            </tbody>            
        <% } else { %> 
            <td>No hay productos cargados.</td>
        <% } %> 
    </table>
    <!-- FOOTER -->
    <%- include("../partials/footer.ejs") %>
    <!-- BOOTSTRAP SCRIPT -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
</body>
</html>